.row
  .col-md-4.col-md-offset-4
    %h1.text-center
      ユーザー登録
    %hr
    = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      .form-inputs
        = f.input :email, required: true, autofocus: true
        = f.input :password, required: true, hint: ("#{@minimum_password_length} 文字以上で入力して下さい" if @minimum_password_length)
        = f.input :password_confirmation, required: true
      .form-actions
        = f.button :submit, '登録', class: 'btn btn-primary btn-block'
    %hr.invisible
    .alert.alert-info
      既にユーザー登録がお済みの方は ,
      %strong= link_to 'こちら', new_user_session_path
