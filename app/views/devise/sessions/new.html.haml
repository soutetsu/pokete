.row
  .col-md-4.col-md-offset-4
    %h1.text-center
      サインイン
    %hr
    = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .form-inputs
        = f.input :email, required: false, autofocus: true
        = f.input :password, required: false
      .form-actions
        = f.button :submit, 'サインイン', class: 'btn btn-primary btn-block'
    %hr.invisible
    .alert.alert-info
      ユーザー登録がまだお済みでない方は ,
      %strong= link_to 'こちら', new_user_registration_path
