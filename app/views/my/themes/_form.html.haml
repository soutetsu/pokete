= simple_form_for(@theme, url: [:my, @theme]) do |f|
  = f.input :uri, label: '画像の url を入力して下さい'
  = f.association :category, label_method: :name, as: :radio_buttons, label: 'カテゴリを選択して下さい'
  = f.submit '投稿', class: 'btn btn-primary'
